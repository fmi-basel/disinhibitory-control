defaults:
  - _self_

loss: ${dataset.loss}

# FB weight modifications
average_fb_weights: False
clip_fb_weights: False
clip_val: 1.0
norm_fb_weights: True
norm_val: 1.0

# FB strength
strong_fb: True
target_nudge: 0.015     # only if strong_fb is False
